1.  Create .env in the backend with keys
    OPENAI_API_KEY=  
    GOOGLE_API_KEY=  
    GROQ_API_KEY=  

    openai | gemini | groq  
    PROVIDER=groq  
    PORT=5000

Run the script npm run dev:server in the backend.  

2.  Create .env in the client with
     NEXT_PUBLIC_BACKEND_URL=
Run the script npm run dev in the client.
